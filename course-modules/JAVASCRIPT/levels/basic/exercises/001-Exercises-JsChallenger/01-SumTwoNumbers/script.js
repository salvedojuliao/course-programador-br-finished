// Write a function that takes two numbers (a and b) as argument
// Sum a and b
// Return the result

function myFunction(a, b){
    let result = a + b;
    return result;
}

alert(myFunction(2, 3));

/*HINT:
You have to differentiate between basic equality (==) and strict equality (===). 
Both operators check whether two values are equal. However, strict equality requires both 
values to have the same type. For example, the string '1' and the number 1 are equal, but not strictly equal.
*/ 