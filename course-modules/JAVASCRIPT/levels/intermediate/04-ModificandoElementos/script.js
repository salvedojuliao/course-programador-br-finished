d = document;
let p = document.getElementById('p1');
// console.log(p);
// p.innerText = " ada";


/* 

innerHTML  
Recebe um novo HTML

innerText 
Mostra só o que está na tela

textContent
Ele mostra todo o conteúdo, inclusive aqueles que estão com display none;

*/

// Modificando um atributo:
// p.className = "Novo nome de Atributo"
// PORÉM, eu só posso modificar atributos clássicos, por exemplo:
// p.style = "color: blue"

// Para ACESSAR o atributo que criei
// p.getAttribute("meuATT");

// Para Mudar o atributo que criei
// p.setAttribute("meuATT", "Outro nome");

// Para CRIAR o atributo que criei
// p.setAttribute("novoAtt", "Qualquer coisa");
// Cria um 'novoATT' com nome 'Qualquer coisa';